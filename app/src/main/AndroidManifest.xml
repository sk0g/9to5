<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dev.a3820team.a9to5">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".WelcomeActivity"
            android:screenOrientation="portrait"
            android:theme="@style/CustomColourScheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MeQuestionsActivity"
            android:screenOrientation="portrait"
            android:theme="@style/CustomColourScheme" />
        <activity
            android:name=".MeResultsActivity"
            android:screenOrientation="portrait"
            android:theme="@style/CustomColourScheme" />
        <activity
            android:name=".OrgQuestionsActivity"
            android:screenOrientation="portrait"
            android:theme="@style/CustomColourScheme" />
        <activity
            android:name=".OrgResultsActivity"
            android:screenOrientation="portrait"
            android:theme="@style/CustomColourScheme" />
        <activity
            android:name=".FinalResultActivity"
            android:screenOrientation="portrait"
            android:theme="@style/CustomColourScheme"/>

    </application>

</manifest>